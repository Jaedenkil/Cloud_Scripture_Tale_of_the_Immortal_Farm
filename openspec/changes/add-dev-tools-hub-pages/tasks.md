## 任务清单（严格对齐 8.4 五步流程）

#### Step 1：三因分析与疑点确认

- [x] 输出 3 个最可能原因（触发条件/可观察现象/架构关联点）：
  - [x] 原因 1：`home` 页缺少开发工具入口，导致工具页面不可达。
  - [x] 原因 2：缺少统一“开发页”聚合层，导致工具入口分散且导航不一致。
  - [x] 原因 3：缺少标准化占位页模板，导致后续工具接入成本高且页面结构不一致。
- [x] 列出并逐项确认不明确项（至少包含）：
  - [x] 开发工具首批按钮清单与显示顺序。
  - [x] `保存` 按钮在占位阶段的预期行为（仅展示/提示/暂不生效）。
  - [x] 占位页标题与功能介绍的文案来源（静态配置或字典映射）。

#### Step 2：零改码验证（不修改现有代码）

- [x] 核对现有 `home` 页面配置，确认是否已有“开发工具”入口与可复用 action。
- [x] 核对 `ui-registry` 与 `ui-flow`，确认是否存在可复用的开发页或工具占位页流转节点。
- [x] 执行相关 flow/ui 测试获取基线结果（不改代码）。
- [x] 记录验证证据（文件路径、节点路径、测试结果），形成进入 Step 3 的依据。

#### Step 3：工程化逐步修改流程与评分

- [x] 输出可执行修改流程（目标/涉及文件/风险与回滚/步骤级验证）：
  - [x] 目标：打通 `home -> 开发页 -> 工具占位页 -> 开发页` 的闭环。
  - [x] 涉及文件：`scenes/ui-registry.yaml`、`scenes/ui-flow.yaml`、`scenes/panel/*.yaml`、相关测试文件。
  - [x] 风险与回滚：若流转异常，按页面粒度回滚新增节点与注册项。
  - [x] 步骤级验证：每新增一个页面即进行结构校验与可达性验证。
- [x] 按 5 个维度评分并记录结论（总分 < 90 必须整改重评）：
  - [x] 根因覆盖完整性（20）
  - [x] 修改路径可执行性（20）
  - [x] 最小改动与架构一致性（20）
  - [x] 回滚与风险控制（20）
  - [x] 验证与验收可测性（20）

#### Step 4：结果汇总与人工确认

- [x] 一次性汇总 Step 1~3 结果（根因、证据、修改流程、评分）。
- [x] 提交给需求方进行人工确认。
- [x] 未确认通过不得进入 Step 5；若驳回则回到 Step 1 重启闭环。

#### Step 5：按审定流程严格执行与报告

- [x] 仅在 Step 4 审核通过后执行实现，禁止擅自偏离审定流程。
- [x] 实施后完成回归验证（页面可达性、返回路径、占位页结构、按钮行为）。
- [x] 输出执行报告（背景/根因/变更清单/验证结果/风险建议）。
- [x] 当前状态：已完成开发与回归验证，等待需求方验收。
