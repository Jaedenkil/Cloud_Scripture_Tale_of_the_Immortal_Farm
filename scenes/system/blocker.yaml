page:
  id: system-blocker-page
  name: System Blocker
  type: overlay
  domain: system
  depthBase: 3000

layers:
  - id: system.blocker
    depth: 3000
    inputPolicy: block

components:
  - id: global-blocker
    type: rectangle
    layer: system.blocker
    style:
      width: "100%"
      height: "100%"
      background: "$colors.panelOverlay"
